{
  "name": "e-commerce-node",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cross-env NODE_ENV=dev node --watch server.js",
    "prod": "cross-env NODE_ENV=pro node --watch server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "compression": "^1.7.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "mongoose": "^8.8.3",
    "process": "^0.11.10"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "helmet": "^8.0.0",
    "morgan": "^1.10.0"
  }
}
